name: CD Staging Deployment

on:
  push:
    branches: [ dev ]

jobs:
  deploy-staging:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Staging Deploy Check
        run: echo "Staging deployment pipeline triggered on dev branch."
